apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-test.fullname" . }}
  labels:
    {{- include "python-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-test.selectorLabels" . | nindent 4 }}
